spring:
  datasource:
    url: ${AUTH_DB_URL:jdbc:postgresql://localhost:5432/fmud}
    username: ${AUTH_DB_USER:fmud_user}
    password: ${AUTH_DB_PASSWORD:change_me}
  flyway:
    enabled: true

logging:
  level:
    org.flywaydb: info
    org.springframework.jdbc: warn
