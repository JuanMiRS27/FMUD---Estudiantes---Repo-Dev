spring:
  datasource:
    url: ${AUTH_DB_URL:jdbc:postgresql://localhost:5432/fmud_auth}
    username: ${AUTH_DB_USER:fmud_auth}
    password: ${AUTH_DB_PASSWORD:fmud_auth_dev}
  flyway:
    enabled: true

logging:
  level:
    org.flywaydb: info
    org.springframework.jdbc: warn
